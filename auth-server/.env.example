# Copy to .env and fill in real values (never commit .env)

GOOGLE_CLIENT_ID=your-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-secret
MONGODB_URI=mongodb://localhost:27017/buildmate_auth_db

# Google Cloud Console — Authorized redirect URI (this machine):
# http://localhost:9000/login/oauth2/code/google
# Optional Authorized JavaScript origin:
# http://localhost:25173
